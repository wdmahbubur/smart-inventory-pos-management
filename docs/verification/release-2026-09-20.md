# Release continuation — 20 September 2026

## Actual outcome

**Application and deployment-tool verification passed. The frontend has NOT been deployed to Vercel.** No production URL or Vercel deployment ID is claimed by this report.

The connected Vercel account can list teams/projects, but the actual `deploy_to_vercel` call returned JSON-RPC `-32602: Tool deploy_to_vercel not found`. Plugin discovery found the installed Vercel integration, not a second usable Vercel deployment action. A repository Actions preflight confirmed that `VERCEL_TOKEN`, `SUPABASE_ACCESS_TOKEN`, and `GEMINI_API_KEY` were not configured. Tokens were not printed or recovered from unrelated applications.

Preflight run: https://github.com/wdmahbubur/smart-inventory-pos-management/actions/runs/35490716023

## Work delivered in this continuation

- `e9c624c72fc1cc618b98c07e28d11d80ef81773d`: credential-presence check without printing secret values.
- `be8112f5586150e069b5ba58baae01cac1268857`: target-scoped Vercel release workflow, dependency-free REST orchestration, hosted read-only HTTP checks, 15 automated deployment-safety tests and release instructions.
- `c0202e93d6b511efccfed4d1e11b935825f0fb16`: include the new tests in ordinary application CI and the public target configuration in the clean source handoff.

The exact approved Supabase target and intentionally public client key are in `config/deployment-target.json`. The scripts reject other repository/branch/project/team targets and reject privileged keys in the public key field. Deployment-management credentials are never copied into client code or Vercel application variables.

The release script is implemented to create/reuse only the correct GitHub-linked Vercel project, choose a verified production domain, configure application environment values, deploy the tested commit, wait for READY and a confirmed production alias, configure exact Supabase callbacks, and run live read-only application checks. It preserves SMTP, confirmation and unrelated Auth settings. These external write paths were tested with HTTP mocks, not successfully exercised against Vercel yet.

## Executed checks

| Check | Result | Evidence / scope |
|---|---|---|
| Deployment guards and REST orchestration tests | 15/15 passed | Local Node test execution and release workflow; deterministic HTTP mocks, not a real deployment. |
| Existing application unit tests | 23/23 passed | Release workflow log contains test count, zero failures. |
| TypeScript, ESLint and production build | Passed | Release workflow `npm run verify`; Next.js 16.3.3, Node 22.16.0, npm 10.9.2. |
| Hosted Supabase HTTP checks | 15/15 passed | Real requests to `galgetikfkyceqkaqdha`; see details below. |
| Application CI including fresh migrations, database tests and benchmark | Passed | Run 35491134851 at c0202e93; all steps completed successfully, including deployment tests, database-role tests, 1,200-product benchmark and production build. |
| Real local Supabase browser suite | Passed | Run 35491076092 at be8112f5; all browser workflow/viewport tests and screenshot artifact steps completed successfully. This is disposable local Supabase on the CI runner, not a deployed frontend. |
| Actual Vercel publish | NOT RUN | Deployment step skipped because the two required Actions secrets are absent. |
| Hosted authenticated Purchase/POS, email delivery, real Gemini generation | Not verified in this continuation | These are separate live-release checks; no fake test account/provider success is substituted. |

Release workflow: https://github.com/wdmahbubur/smart-inventory-pos-management/actions/runs/35491076004

Application CI: https://github.com/wdmahbubur/smart-inventory-pos-management/actions/runs/35491134851

Browser CI: https://github.com/wdmahbubur/smart-inventory-pos-management/actions/runs/35491076092

The release workflow has a successful verification job and a successful **credential-check job**, but its actual deployment step is **skipped**. The summary explicitly says **NOT DEPLOYED — authorization required**. A green check on that workflow must not be interpreted as a live site.

The CI install reported zero npm-audit vulnerabilities for the resolved dependency set at that time. It also reported deprecated ESLint/Action runtime notices. This is not a comprehensive security audit or certification of future dependencies.

## Real hosted HTTP verification

At 2026-09-20 05:13:02 UTC, `node scripts/check-hosted-public.mjs` completed:

1. Supabase Auth settings endpoint reachable with the selected publishable client key.
2. Anonymous SELECT denied on all twelve public application tables: profiles, stores, categories, suppliers, products, inventory_balances, purchases, purchase_items, sales, sale_items, stock_movements and ai_insights.
3. Anonymous invocation denied for two read-only RPCs: get_workspace and get_insight_context.

Table requests used `limit=0`; no rows were downloaded. RPC requests were read-only. No signup, checkout, stock change, email, provider request, seed or migration was performed by these HTTP checks.

Evidence artifact: https://github.com/wdmahbubur/smart-inventory-pos-management/actions/runs/35491076004/artifacts/10598644844

Artifact ZIP SHA-256: `274219e71ad04bb31d898018f5c205ad99a23a1ea14ac5abcd57bcd6450d0873`. Artifacts have limited retention; the run logs are the accompanying execution record.

## Data preservation

The hosted read preflight also confirmed twelve public tables, no public table without RLS, ten recorded application source migrations, and the presence of both protected legacy schemas. It found zero new profiles, stores, products, purchases and sales. The archived original tables and independent in-database copies created in the previous cutover were not modified by this continuation. This is not a new offsite backup or a re-import of legacy accounts/stock.

## Exact remaining setup

In this GitHub repository, open **Settings → Secrets and variables → Actions → New repository secret**. Add `VERCEL_TOKEN` authorized for `wdmahbuburs-projects` and `SUPABASE_ACCESS_TOKEN` authorized for the selected inventory project. Use short-lived credentials and never paste them into chat, source files or issues.

Then open **Actions → Deploy Smart Inventory to Vercel → Run workflow → master**. Adding the secrets alone does not trigger execution. The script retrieves real project/domain information; no guessed live URL is supplied here. Details and safety boundaries: [Vercel release guide](../deployments/VERCEL_RELEASE.md).

The Supabase management token is used to check the approved project and configure its Site URL/callbacks; it is not a database password, not the application's publishable key, and not sent to the frontend. Gemini is optional: a real server-side key and explicitly chosen model are required only for successful AI generation. General public registration also requires a working production email configuration. Live authenticated business/browser and provider smoke tests must be recorded after deployment before the full PRD is marked complete.
