import {Heading,Stat,Card,Table,Pagination,Empty,Notice,ReportTabs,ProductLabel,Status} from '@/components/ui';
import {ValueBars,StockDonut} from '@/components/charts';
import {ReportPeriod,ExportButton} from './report-controls';
import {FiltersBar} from '@/components/filters';
import {CatalogFilter} from './catalog-filter';
import {report,entity} from '@/lib/server/data';
import {parseFilters} from '@/lib/schemas';
import {money} from '@/lib/money';
import {displayDate} from '@/lib/dates';
import type {ReportKind,Category} from '@/lib/domain';
import type {SearchParams} from './catalog-pages';
export async function ReportPage({kind,params}:{kind:ReportKind;params:SearchParams}){
 const filters=parseFilters(params);const data=await report(kind,filters);const inventory=kind==='inventory',sales=kind==='sales';const category=inventory&&filters.category?await entity<Category>('categories',filters.category):null;
 const title=inventory?'Inventory report':sales?'Sales report':'Purchase report';
 const rows=data.chart.map(row=>({id:row.id,name:row.name,value:inventory?row.value_paisa:sales?row.gross_paisa??'0':row.total_paisa??'0'}));
 return <><Heading eyebrow="Insights / Reports" title={title} description={inventory?`Current snapshot as of ${displayDate(data.snapshot_at,true)} · Asia/Dhaka`:`${displayDate(data.from!)} – ${displayDate(data.to!)} · Actual ${sales?'completion':'receipt'} dates in Asia/Dhaka`} actions={<ExportButton kind={kind}/>}/><ReportTabs active={kind}/>{!inventory&&<ReportPeriod from={data.from!} to={data.to!}/>}<div className={`stats ${inventory||!sales?'three':''}`}>{inventory?<><Stat label="Estimated stock value" value={money(data.summary.value_paisa)} note="All active products · reference-cost estimate"/><Stat label="Active products" value={data.summary.active_count} note="Current whole catalog" color="blue"/><Stat label="Products needing attention" value={data.summary.attention_count} note={`${data.summary.low_stock} low · ${data.summary.out_of_stock} out of stock`} color="amber"/></>:<><Stat label={sales?'Net sales':'Received purchase value'} value={money(data.summary.total_paisa)} note={sales?'After fixed order discounts':'Goods received, not payments confirmed'} icon={sales?'store':'bag'}/><Stat label={sales?'Completed sales':'Received purchases'} value={data.summary.count} note={sales?'Completed cash sales only':'Drafts excluded'} color="blue"/><Stat label={sales?'Units sold':'Units received'} value={data.summary.units} note="Selling units, not weight or volume" color="amber"/>{sales&&<Stat label="Order discounts" value={money(data.summary.discount_paisa??'0')} note={`Average sale: ${money(data.summary.average_paisa??'0')}`} color="rose"/>}</>}</div><div className="report-columns" style={{marginBottom:22}}><Card title={inventory?'Estimated value by category':sales?'Product sales before discounts':'Received spending by product'} description={inventory?'Current category and reference-cost estimate.':'Top 10 products by value in this period; all products are in the paginated table.'} body><ValueBars rows={rows} label={inventory?'Current category value estimates':sales?'Gross product sales':'Actual received product spending'}/></Card><Card title={inventory?'Stock status':sales?'Sales reconciliation':'Received purchase summary'} body>{inventory?<><StockDonut inStock={data.summary.in_stock} low={data.summary.low_stock} out={data.summary.out_of_stock}/><Notice>Zero quantity is out of stock. Positive quantity below minimum is low. Equality to minimum is in stock.</Notice></>:sales?<><dl className="definition-list"><dt>Gross product sales</dt><dd>{money(data.summary.subtotal_paisa??'0')}</dd><dt>Less order discounts</dt><dd>−{money(data.summary.discount_paisa??'0')}</dd></dl><hr/><dl className="definition-list"><dt className="strong">Net sales</dt><dd className="total">{money(data.summary.total_paisa)}</dd></dl><div style={{marginTop:22}}><Notice>Product bars show gross line sales before order discounts. Tender and change are not revenue. No profit is calculated.</Notice></div></>:<><dl className="definition-list"><dt>Received value</dt><dd>{money(data.summary.total_paisa)}</dd><dt>Received documents</dt><dd>{data.summary.count}</dd><dt>Units received</dt><dd>{data.summary.units}</dd></dl><div style={{marginTop:24}}><Notice>Actual captured item costs are used here, not the current catalog reference cost. Drafts never enter these totals.</Notice></div></>}</Card></div>{inventory&&<FiltersBar stock><CatalogFilter initial={category}/></FiltersBar>}<Card title={inventory?'Current inventory snapshot':sales?'Product sales breakdown':'Product purchase breakdown'} description={inventory?'Table filters affect the rows and filtered estimate, not the global summary cards.':'Products are grouped by immutable ID; labels use their latest snapshot in this period.'}><Table headers={inventory?['Product','Category','Available','Minimum','Reference cost','Estimated value','Status']:sales?['Product','SKU','Quantity sold','Gross line sales']:['Product','SKU','Quantity received','Actual unit cost','Received value']} empty={!data.rows.length?<Empty title="No matching report rows" description="Choose another period or clear filters. Empty totals remain zero."/>:undefined}>{data.rows.map(row=><tr key={row.id}><td><ProductLabel name={row.name} sku={inventory?row.sku:undefined} icon_key={row.icon_key} color_key={row.color_key}/></td>{inventory?<><td>{row.category_name}</td><td>{row.quantity} {row.unit}</td><td>{row.minimum_stock}</td><td>{money(row.reference_cost_paisa)}</td><td><strong>{money(row.stock_value_paisa)}</strong></td><td><Status status={row.stock_status}/></>:<><td>{row.sku}</td><td>{row.quantity}</td>{!sales&&<td>{row.min_cost_paisa===row.max_cost_paisa?money(row.min_cost_paisa??'0'):<span>Various costs<small className="muted" style={{display:'block'}}>{money(row.min_cost_paisa??'0')}–{money(row.max_cost_paisa??'0')}</small></span>}</td>}<td><strong>{money((sales?row.gross_paisa:row.total_paisa)??'0')}</strong></td></>}</tr>)}</Table>{inventory&&<div className="card-footer between"><span className="muted small">Filtered estimate · all {data.total} matching products</span><strong>{money(data.filtered_value_paisa??'0')}</strong></div>}<Pagination data={data} path={`/reports/${kind}`} params={params}/></Card>{!inventory&&!sales&&<div style={{marginTop:22}}><Card title="Supplier breakdown"><Table headers={['Supplier','Received purchases','Received value']} empty={!data.suppliers?.length?<Empty title="No received purchases in this period"/>:undefined}>{data.suppliers?.map(s=><tr key={s.id}><td><strong>{s.name}</strong></td><td>{s.count}</td><td>{money(s.total_paisa)}</td></tr>)}</Table></Card></div>}<div style={{marginTop:18}}><Notice>{inventory?'This is a current operational stock estimate, not historical accounting valuation. Exports capture a fresh consistent snapshot at download time.':sales?'CSV exports include every matching sale line (up to 100,000 rows). Each order discount appears only on its first stable line, so summing the discount column cannot double count it.':'CSV exports include every matching received line (up to 100,000 rows), its actual cost, invoice date and receipt timestamp.'}</Notice></div></>;
}
